# Plan Execution Report

Durable execution ledger for the architecture (docs/architecture/) and implementation
(plans/) campaigns. Every numbered plan has a requirement-level row; the matrix below is
kept truthful as work lands. The final column of each row carries commit hashes.

- Campaign baseline commit: `0d62d2e` (untracked `plans/` audited and committed alone:
  exactly README + nine numbered plans, no secrets, no unrelated material).
- Working branch: `main`, no pushes, no history rewrites.
- **Campaign complete: 2026-08-12.** All seven architecture plans (01–07) and all nine
  implementation plans (001–009) are SHIPPED; the architecture rows below were filled by
  the final cross-plan audit, which grep-verified every claim against live code on the
  same day. The four repository gates are green at the counts in the table below.

## Requirement matrix — architecture plans (docs/architecture/)

| Plan | Requirement | Status | Source evidence | Changes | Tests | Review | Commit |
|------|-------------|--------|-----------------|---------|-------|--------|--------|
| 01 | `AgentBackend` contract (`agents/types.ts`) | SHIPPED | types.ts: `AgentBackend` interface with id/displayName/capabilities/hasModelCatalog/hasSlashCommands, launch/resume/sessionRoot/ownsSessionPath/resolveSessionPath/readTranscript/extractQuestions/answerQuestion/control/models/commands (+ optional deliverInitialPrompt, renameStoredSession); `ControlAction` union abort|retry|compact|fork|rename|close|set_model|set_thinking | | (see evidence) | final audit 2026-08-12 | 688c020 |
| 01 | Registry (`backendFor*`, `knownBackends`) | SHIPPED | registry.ts: all four functions exported and used — backendFor (throws on unknown), backendForSessionPath (routes by ownsSessionPath), backendForAgentSessionInfo (id/path kinds), knownBackends (pi + claude registered at module load) | | (see evidence) | final audit 2026-08-12 | 688c020 |
| 01 | Pi adapter; claude adapter | SHIPPED | agents/pi/ (index, transcript, models, commands) and agents/claude/ (index, transcript); claude resumes by session id, resolves `kind: "id"` via bounded walk of the projects root (MAX_SCAN_DEPTH 3 / MAX_SCAN_FILES 5000), sessionRoot honors CLAUDECONFIGDIR | | (see evidence) | final audit 2026-08-12 | 688c020 |
| 01 | Session orchestration agent-agnostic (`sessions.ts`) | SHIPPED | sessions.ts has zero pi imports (grep-verified); adapters never import sessions.ts (shell.ts + herdr/panes.ts break the cycle); the pi npm dependency lives only inside the pi adapter | | (see evidence) | final audit 2026-08-12 | 688c020 |
| 01 | Multi-backend catalog + fair scan budget | SHIPPED | session-catalog.ts: MAX_CANDIDATES 2000 split evenly per root (perRoot = MAX_CANDIDATES / roots.length, min 1) so a giant pi store cannot starve the claude root; scanning capped | | (see evidence) | final audit 2026-08-12 | 688c020 |
| 01 | Wire: `agentKind`/`capabilities`/`displayName`; `/api/agents/kinds`; `agent` query param | SHIPPED | routes/agents.ts: GET /api/agents/kinds returns {id, displayName, capabilities, hasModelCatalog, hasSlashCommands} per known backend; AgentCard gains agentKind/capabilities/displayName (agentKind: backend?.id ?? agent); models.ts/commands.ts take ?agent= defaulting to pi with 404 on unknown (pinned by plan 008); SessionReadResult gains agentKind; Android data/Models.kt mirrors the fields | | (see evidence) | final audit 2026-08-12 | 688c020 |
| 01 | Control dispatch through backend + capability enforcement | SHIPPED | sessions.ts controlSession: abort/retry/compact/fork pane-direct; rename/close/set_model/set_thinking resolve the pane's backend and delegate via backend.control; capabilities.has gate before delegation (400 naming the backend, plan 004 hardened); BridgeError statuses 404/409 preserved | | (see evidence) | final audit 2026-08-12 | 688c020 |
| 01 | Android backend-aware (models(agent), commands, session actions, gates) | SHIPPED | BridgeClient: models(agent)/commands(cwd, agent)/createSession(agent)/agentKinds(); ChatViewModel capability-gated model fetch + agentKind propagation; NewSessionSheet BackendSection (hidden until ≥2 kinds, model pick hidden for catalog-less); ChatScreen capability-gated action menu; HistoryScreen gates rename/fork on agentKind == "pi" | | (see evidence) | final audit 2026-08-12 | 688c020 |
| 02 | One `transcript.ts` module owning the record vocabulary | SHIPPED | transcript.ts exports TranscriptEntry/Transcript/TranscriptReadOpts plus the shared block vocabulary (TextBlock/ThinkingBlock/ToolCallBlock/ContentBlock/TokenUsage/TranscriptRole); both adapters parse into it (agents/pi/transcript.ts, agents/claude/transcript.ts) | | (see evidence) | final audit 2026-08-12 | f48c82c |
| 02 | Three read modes (`tail` bounded, `metadataOnly`, full) + cross-mode agreement | SHIPPED | readTranscript(path, { tail }) reads a bounded byte window from the end (HEAD_WINDOW_BYTES 128 KiB head for metadata); metadataOnly skips content blocks; transcript.test.ts has cross-mode agreement tests: 'all three modes agree on the metadata of one file' and 'all three file read modes agree on a small file' | | (see evidence) | final audit 2026-08-12 | f48c82c |
| 02 | `writeSessionTitle` in transcript module | SHIPPED | the record write moved out of the catalog: writePiSessionTitle lives in agents/pi/transcript.ts beside its parser and is reached through the backend's renameStoredSession (pi adapter; claude advertises none — its JSONL has no title records, the app hides the action) | | (see evidence) | final audit 2026-08-12 | f48c82c |
| 02 | Consumers ported (`questions.ts`, catalog, board-detail, `server.ts`) | SHIPPED | questions.ts, session-catalog.ts, board-detail.ts (reads via readTranscript tail mode — its private parser is gone), routes/sessions.ts and the adapters all consume ../transcript.js (grep-verified); board-detail.ts:38 backend.readTranscript(path, { tail }) | | (see evidence) | final audit 2026-08-12 | f48c82c |
| 02 | Old parsers deleted; test corpus merged with cross-mode tests | SHIPPED | bridge/src/pi no longer exists; three parser test files collapsed into transcript.test.ts which keeps the prior assertions and adds the cross-mode agreement tests | | (see evidence) | final audit 2026-08-12 | f48c82c |
| 03 | `HerdrPort` declared; `HerdrClient implements HerdrPort` | SHIPPED | herdr/port.ts declares the interface; client.ts:273 `HerdrClient implements HerdrPort`; sessions.ts/server.ts/adapters take HerdrPort | | (see evidence) | final audit 2026-08-12 | 3c612bc, 86a5b7a |
| 03 | Shared `fakeHerdr` in test support | SHIPPED | test/support/fake-herdr.ts exports fakeHerdr() returning HerdrPort & { sent, setSnapshot, failNext }; used by 8+ suites (agents-claude, live-output, review, server, sessions-http, sessions, ws-commands, herdr-client) replacing the ad-hoc sessions-http fake | | (see evidence) | final audit 2026-08-12 | 3c612bc, 86a5b7a |
| 03 | Route table + dispatcher; feature-grouped `routes/` handlers; auth/body/404 in dispatcher | SHIPPED | routes/index.ts buildRoutes(): Route[]; 13 feature modules under routes/ (agents, attachments, catalog, commands, dirs, health, models, review, sessions, usage…); dispatcher.ts owns isAuthorized, JSON body parsing, the 404 fallback, and error→status mapping; RouteTable rejects shadowing patterns (startup assertion test at server.test.ts:478) | | (see evidence) | final audit 2026-08-12 | 3c612bc, 86a5b7a |
| 03 | No skip gate; live-socket cases gated explicitly | SHIPPED | server.test.ts is the always-running offline suite (no skip gate); live coverage lives in server.integration.test.ts gated on an EXPLICIT HERDR_SOCKET_PATH opt-in with a skip notice (plan 008 later tightened the gate to require the socket to exist) | | (see evidence) | final audit 2026-08-12 | 3c612bc, 86a5b7a |
| 03 | Offline tests for cards/paths/workspace roots/WS commands; config/status tests | SHIPPED | derived-views.test.ts: deriveAgentCards + snapshotPaths suites; sessionWorkspaceRoots exercised through review tests; ws-commands.test.ts offline WS path; config.test.ts (XDG_CONFIG_HOME, generateToken length rule) and status.test.ts offline | | (see evidence) | final audit 2026-08-12 | 3c612bc, 86a5b7a |
| 04 | `CockpitApi` interface; `BridgeClient : CockpitApi` | SHIPPED | net/CockpitApi.kt: 22 suspend funs + connectedHost, defaults on interface only | net/BridgeClient.kt implements it; `BridgeException(status, reason)`; `call()` private with body; `post()` deleted; upload folded into `call()` | 491e50c | | 491e50c |
| 04 | ViewModels/container/ReplyReceiver consume `CockpitApi` | SHIPPED | all 9 state ViewModels take CockpitApi; AppContainer.bridge: CockpitApi; ReplyReceiver steers via container | grep-verified clean | 491e50c | | 491e50c |
| 04 | `FakeCockpitApi` shared by test source sets; MockWebServer tests migrated | SHIPPED | app/src/commonTest/.../FakeCockpitApi.kt wired into test+androidTest in build.gradle.kts; 8 state/ test files migrated | 137 unit tests pass | 491e50c | | 491e50c |
| 04 | WS path tests; `BridgeException`; auth folded; `call`/`post` private | SHIPPED | BridgeClientWsTest: steer/runSlashCommand/answerQuestion frames, error frame → IOException, feed skipping, empty-key omission; BridgeClientUploadTest kept as HTTP contract test | 137 unit tests pass | 491e50c | | 491e50c |
| 04 | AGENTS.md gotcha deleted | SHIPPED | AGENTS.md net/ map reads 'BridgeClient behind the CockpitApi interface'; fake noted | | 491e50c | | 491e50c |
| 05 | `SessionAction` enum both sides; call sites converted | SHIPPED | `data/SessionAction.kt` (wire+label, `fromWire` drops unknowns); CockpitApi/controlSession take the enum; all 7 call sites converted | | | | 20bb93b |
| 05 | `CatalogAction` for resume/fork/rename | SHIPPED | `data/CatalogAction.kt` (Resume/Fork/Rename/Delete — delete added because the enum must declare the full catalog verb set); `sessionCatalogAction` typed | | | | 20bb93b |
| 05 | Capabilities decoded into UI state; menu rendered from set | SHIPPED | `toSessionActions()` decode helper; overflow menu renders from the set in enum declaration order; thinking chip gated via `canSetThinking` (plan 01) | | | | 20bb93b |
| 05 | Server-side capability enforcement in `controlSession` | SHIPPED | claude adapter rejects out-of-capability verbs with 'unsupported control action for claude' before pane input; pi adapter covers the pi surface (enforcement itself shipped with plan 01, 688c020; verified again here) | | | | 20bb93b |
| 06 | `Poller` + tests; four loops migrated | SHIPPED | `state/Poller.kt`; Board (3s), SessionHistory (8s), Chat (2.5s), Usage (10s) use it; `PollerTest` (immediate tick, restart cancels, stop, scope death) | | | | fad4d24 |
| 06 | `Loadable` + `FailureKind` (from `BridgeException`); VMs migrated; offline rule explicit | SHIPPED | `state/Loadable.kt`; Usage/Connect/Board/SessionHistory/Chat/Review migrated per-field; Usage chart-never-blanks rule explicit in `UsageViewModel` + test | | | | fad4d24 |
| 06 | `viewModelFactory` helper; eight factories collapsed | SHIPPED | `state/ViewModelFactory.kt`; Board/Chat/Connect/SessionHistory collapsed; CommandPalette/LiveOutput/NewSession/Review retained by decision (blast radius) | | | | fad4d24 |
| 07 | `Destination` in `ui/nav/Destination.kt` with `routes`/`forRoute` | SHIPPED | ui/nav/Destination.kt: enum Board/Sessions/Usage/Review with `routes` set + `forRoute`; Routes in MainActivity trimmed to connect/chat/live/settings | | | | d46a6a4 |
| 07 | Bottom-bar check derived; `Routes` trimmed | SHIPPED | CockpitAppNav bar visibility is `currentRoute in Destination.routes`; tab routes flow through `Destination.X.route` everywhere (startDestination, popUpTo, composable(), onTab) | | | | d46a6a4 |
| 07 | `TabScaffold` extracted; four tabs use it | SHIPPED | ui/nav/TabScaffold.kt (zero-inset Scaffold + AppTopBar, optional FAB slot); Board/Sessions/Usage/Review blocks all use it; AppTopBar moved to ui/components/AppTopBar.kt (was private in MainActivity) | | | | d46a6a4 |
| 07 | Unit tests (routes/forRoute) + NavHost graph test | SHIPPED | DestinationTest (4 unit tests); NavHostGraphTest launches the real MainActivity (seeded connection + monitoring reset), asserts 4 tabs and tap-through; inset contract verified structurally + on-device (GMD 89/89) | | | | d46a6a4 |
| 07 | POST_NOTIFICATIONS gated on monitoring (test blocker fix) | SHIPPED | MainActivity requests the permission only when MonitoringStore.enabled; Settings toggle requests it before starting the FGS (was: dialog on every cold start, and a FGS SecurityException if granted never arrived); enables real-activity NavHost tests | | | | d46a6a4 |

## Requirement matrix — implementation plans (plans/)

| Plan | Requirement | Status | Source evidence | Changes | Tests | Review | Commit |
|------|-------------|--------|-----------------|---------|-------|--------|--------|
| 001 | Step 1: `herdrSubscribe` settles on every failure path (error/close-before-ack/ack-timeout/error-ack) instead of wedging | SHIPPED | client.ts `settled` flag + `fail()` (clears ackTimeout, sets `closed` to suppress onClose, destroys, rejects); post-resolve errors still call `callbacks.onError` | **Deviation:** resolve happens on a *parsed ack* inside the data handler, not the plan's literal `sock.once("data")` — the plan's done criterion says to confirm the resolve path coexists with the reject paths; intent satisfied | 6 offline subscribe tests (missing path, close-before-ack, stall→3s reject, bad ack, late-ack-after-stop, feed retry) + live suite (0 skipped) | fresh-agent review: strict ack validation (id echo + `subscription_started`, live-probed) | a844b69 |
| 001 | Step 2: `herdrRequest` rejects on empty close | SHIPPED | client.ts `finish()`: `first === undefined` guard rejects with "herdr closed the connection without responding" (was: parse `"{}"` → resolve `undefined`) | | offline test: close-with-no-bytes rejects | | a844b69 |
| 001 | Step 3: feed retries failed rebuild with backoff; retry handle stored and cleared in `stop()` | SHIPPED | feed.ts `SUBSCRIBE_RETRY_MS = 1000`; `retryTimer` field; subscribe wrapped in try/catch emitting `feed_error` + scheduling retry guarded by `stopped`; **review hardening:** `rebuildTimer` + `reconnectTimer` also tracked and cleared in `stop()` (stale timers cannot rebuild a restarted feed) | | offline test: restart after socket death → exactly 2 subscribes, no spurious rebuild | fresh-agent review finding 3 fixed | a844b69 |
| 001 | Step 4: snapshot re-synced at top of `doBuildSubscription` | SHIPPED | feed.ts `try { await this.refreshSnapshot(true) } catch {}` then re-check `stopped`; **review hardening:** `refreshSeq` generation guard so an older in-flight refresh cannot overwrite a newer snapshot | | | fresh-agent review finding 4 fixed | a844b69 |
| 001 | Step 5: crash-proof HTTP/WS handlers | SHIPPED | server.ts: async `createServer` callback wrapped (500 JSON only if `!response.writableEnded`, secondary write failures swallowed); `wss.on("error")` + `ws.on("error")` (log + same cleanup as close); `handleFeed` guards `ws.send` with `readyState === OPEN` | | server.test.ts offline HTTP suite exercises the path | | a844b69 |
| 001 | Step 6: pane-close kind sets + tracker prune | SHIPPED | server.ts `STATUS_KINDS`/`CLOSE_KINDS` explicit snake+dot sets (no blind `replace(/_/g,'.')`); close branch runs `tracker.prune(live pane ids)` + `boardDetail.prune(snapshotPaths(...))`; `grep -n tracker.prune src/server.ts` → exactly 1 match | **Deviation:** server-level prune assertion in `server.test.ts` per the plan's explicit alternative (test-plan item 5), instead of `status.test.ts` | server.test.ts: pane_exited + pane_agent_status_changed prune tests | reviewer instructed not to flag server.test.ts (plan permits it) | a844b69 |
| 002 | Step 1: narrow the config catch — only missing/unparseable mints a token; a failed re-persist of a valid config keeps the token | SHIPPED | config.ts `readOk` flag; write failure with `readOk` → log + return parsed config; fresh-config write failure → throw (nothing to pair against) | | config.test.ts: file made read-only (0o400) so `writeFile` genuinely fails EACCES; token + port preserved; warning emitted (grep-verified) | fresh-agent review: test did not reach the failure branch (dir chmod does not block truncation of an existing file) → fixed by chmod-ing the file itself | 569acbb |
| 002 | Step 2: remove the token from the serve banner | SHIPPED | cli.ts: `token: run 'cockpit-bridge pair' or read <path>`; `grep -rn 'token: ${config.token}' bridge/src/` → nothing | | | | 569acbb |
| 002 | Step 3: restrict query-token auth to the WS upgrade | SHIPPED | dispatcher.ts `isAuthorized(request, token, opts: { allowQueryToken? })`; server.ts upgrade passes `{ allowQueryToken: true }`; `dispatchRoute` passes nothing | STOP-condition check: BridgeClient.kt uses `?token=` only for the WS connect (line 310), all HTTP via Bearer header | server.test.ts: `GET /api/health?token=<token>` → 401; existing ws test still asserts the query-token upgrade succeeds | | 569acbb |
| 002 | Step 4: Android backup exclusion for the connection prefs | SHIPPED | `res/xml/backup_rules.xml` + `data_extraction_rules.xml` exclude `sharedpref cockpit_connection.xml`; manifest carries both attributes; prefs file name grep-verified against ConnectionStore.kt | | Android unit tests (BUILD SUCCESSFUL) + assembleDebug — manifest/XML change, no new unit tests per plan | | 569acbb |
| 003 | Step 1: `readSlice` loops until the window is full or EOF | SHIPPED | transcript.ts: `bytesRead` accumulate + `buffer.subarray(0, total)` — short reads no longer NUL-pad the JSON parser | | transcript.test.ts: `Buffer.byteLength(text) === file length` + 3 entries parsed | | 5a89196 |
| 003 | Step 2: transcript memo keyed (mtimeMs, size) on the chat read path | SHIPPED | routes/sessions.ts `readTranscriptMemoized` (cap 8, FIFO eviction) — steady-state chat poll costs one stat, not a multi-MB read+parse | STOP-condition check: `extractQuestions` builds only new arrays; `readSession` only slices — sharing the parsed Transcript is safe | session-read.test.ts: unchanged file returns the same entry *objects* (identity, timing-free); append invalidates (size shift) | | 5a89196 |
| 003 | Step 3: catalog memo keyed (mtimeMs, size) per file | SHIPPED | session-catalog.ts `catalogMemo` (cap 600, just above MAX_SCANNED_FILES); `SessionFile.size` recorded in all three stat sites; null results memoized deliberately (readability flips change mtime/size) | | session-catalog.test.ts: unchanged store returns identical metadata; new file shifts keys → fresh listing | | 5a89196 |
| 003 | Step 4: tolerance guards around active-refs stat, nested readdir, addSessionFile | SHIPPED | session-catalog.ts: stat → `continue`, readdir → `continue`, `addSessionFile` body → swallow; a bad entry can no longer abort the whole scan (was: 500 on Sessions tab) | | session-catalog.test.ts: dangling symlink tolerated; unreadable subdir (0o000, root-skipped) skipped | | 5a89196 |
| 003 | Step 5: git-root memo, parallel cwds, reviewRoots TTL with in-flight coalescing | SHIPPED | routes/review.ts: `gitRepoRootCached` per canonical cwd (cap 500, null cached too); `Promise.all` over collected cwds; `REVIEW_ROOTS_TTL_MS = 2_000` global with shared in-flight promise so the Review-open burst shares one scan | **Deviation (user-approved):** the TTL keeps a just-removed workspace reviewable ≤2s; server.test.ts pinned instant revocation and was updated to assert in-window 200 + post-TTL 403 | review.test.ts: concurrent burst all-200; TTL window/expiry legs; server.test.ts revocation test adapted | fresh-agent review finding (concurrent first-wave requests still scanned separately) → in-flight coalescing + burst test | 5a89196 |
| 004 | Step 1: one shared pane→backend resolver | SHIPPED | agents/registry.ts `resolveBackendForPane` (three-step chain, sync, null on miss); commands.ts feeds it the feed snapshot, sessions.ts `await herdr.snapshot()` keeping its throw-404 policy | | | fresh-agent review: clean | 35fc162 |
| 004 | Step 2: validate `send_text` and `steer` | SHIPPED | commands.ts: send_text rejects (not alters) newline/control/length > MAX_ANSWER_LENGTH text; steer rejects NUL/DEL or > MAX_PROMPT_LENGTH, multi-line accepted; `MAX_PROMPT_LENGTH`/`PROMPT_FORBIDDEN_CHAR` exported from sessions.ts | | ws-commands: send_text newline/NUL/empty → 400-class throws; steer multi-line accepted, NUL rejected | fresh-agent review finding (empty steer text hit the legacy message instead of the mandated one) → fixed by separating target/text validation | 35fc162 |
| 004 | Step 3: drop the keys-replay generic fallback | SHIPPED | commands.ts: unknown agents get type+Enter only; keys without text rejected ('requires text for unknown agents') | | ws-commands: unknown+keys rejection; keys-only and keys+text cases moved to the pi backend path | | 35fc162 |
| 004 | Step 4: route-level action validation + capability enforcement | SHIPPED | routes/sessions.ts `CONTROL_ACTIONS` satisfies readonly ControlAction[] (no more `body.action as never`); sessions.ts `backend.capabilities.has(action)` before `backend.control`, 400 naming the backend; abort fast path untouched | **Contract-pin update:** sessions.test.ts capability message pins (outside scope list) updated to the new check — STOP condition checked: CLAUDE_CAPABILITIES genuinely lacks fork/retry, so the pins were the old switch-default message, not a product conflict | sessions-http: unknown action 400, claude+fork 400 with backend named, missing action 400 | | 35fc162 |
| 004 | Step 5: correct status codes from `readSessionRoute` | SHIPPED | routes/sessions.ts throws `SessionsError(..., 403)` for outside-store paths; local try/catch deleted — dispatcher maps 403 and 502 | **Contract-pin update:** server.test.ts path-guard pin 500→403 (plan's own anticipated branch: the pin covered the exact case the plan converts) | sessions-http: outside-store read → 403 | | 35fc162 |
| 004 | Step 6: match answers by `questionIndex` | SHIPPED | questions.ts `byIndex` map per call id; positional fallback only when no answer in the call carries an index | | questions: out-of-order index attach; partially-indexed call never falls back | | 35fc162 |
| 005 | Step 1: lifecycle-scoped polling wrappers on Chat/Board/History VMs | SHIPPED | startPolling()/stopPolling() with a lifecycleActive guard (true no-op when already polling); Poller's immediate first tick doubles as the first paint, so the init one-shot refresh is gone; BoardViewModel.connect() restarts loops only while the board is visible | **Drift (planned at 1ece5c9, before architecture 06):** Poller.kt already shipped the loop mechanics; the plan anticipated this and declared the wrapper shape compatible. Keeping the plan's literal init one-shot was rejected on fresh-agent review: it raced Poller's immediate tick and violated Poller's documented no-first-refresh contract | PollLifecycleTest: no polling until started; second start is a no-op; stop halts; resume restarts | fresh-agent review: High (late health probe resurrected board loops after stop) + Medium (overlapping init refreshes) + Medium (wrappers restarted instead of no-op) — all fixed as prescribed; follow-up review: no findings | 6410595 |
| 005 | Step 2: LifecycleStartEffect wiring in the three screens | SHIPPED | ChatScreen/BoardScreen/HistoryScreen: `LifecycleStartEffect(Unit) { startPolling(); onStopOrDispose { stopPolling() } }` (pattern from LiveOutputPanel); ConnectScreen STOP-condition checked: it owns ConnectViewModel, BoardViewModel only lives under BoardScreen | | emulator suite covers the STARTED path for all three screens | | 6410595 |
| 005 | Step 3: cancellation is never rendered as failure | SHIPPED | all 36 `catch (Exception)` sites in state/ rethrow `CancellationException` first; the 2 IOException sites were already safe (CancellationException is a RuntimeException); no site wraps pure parsing | | PollLifecycleTest.cancelledRefreshDoesNotWriteFailure: scope cancelled mid-refresh → transcript never Loadable.Failed | | 6410595 |
| 005 | Step 4: pushed deep links are validated payload | SHIPPED | service/DeepLink.kt hosts `resolveNotificationLink` + `statusForTitle`; message.click runs through parseCockpitUri and is rebuilt canonically via cockpitChatUri; parsed paneId beats the raw payload field for the reply action; invalid click falls back to raw paneId; neither → notification skipped (matches the old ?: return); CockpitMonitorService.showEventNotification and CockpitApp.showAgentNotification both consume it | **Deferral:** the service notification flow has no unit-test seam until plan 007's DI refactor — the pure helper is tested directly instead (plan-sanctioned helper extraction; DeepLink.kt is its home) | DeepLinkValidationTest (7 cases: foreign click falls back, null without paneId, parsed click beats raw paneId, no-click fallback with title-derived status, scheme rejection) | | 6410595 |
| 006 | Step 1: AGENTS.md architecture map + extendability path + test count | SHIPPED | bridge bullet names herdr/ (incl. `port.ts` seam), `routes/`, `agents/` (+ `pi/`, `claude/`, `types.ts` interface), per-feature modules, and the fakes in `bridge/test/support/` with each fake's interface (`fake-herdr` → HerdrPort, `fake-feed` → HerdrEventFeed); extendability bullet points at `bridge/src/agents/`; the only test count left in the repo is 286 tests / 43 suites in the verification section | **Drift:** AGENTS.md had 2 lines changed since the plan was written (architecture 04/07 already truthier); re-verified every claim live first. dev-workflow.md count dropped per plan item 6 (its done-criterion file list is contradicted by item 6 itself) | verification greps: no `bridge/src/pi`, no stale counts | fresh-agent review: fake-feed seam described as behind HerdrPort (wrong — it implements HerdrEventFeed) → fixed in 6bd86dc; one finding quoted nonexistent text (dismissed) | 3a3e138, 6bd86dc |
| 006 | Step 2: architecture index status table | SHIPPED | all seven plans marked Shipped with commit hashes (1 `688c020`, 2 `f48c82c`, 3 `3c612bc`+`86a5b7a`, 4 `491e50c`, 5 `20bb93b`, 6 `fad4d24`, 7 `d46a6a4`); Top recommendation rewritten as a historical record; live planning pointed at `plans/` + execution ledger | **Drift:** the plan's "mark 1 and 3, live recommendation is plan 4" is superseded — phase 1 shipped 4–7 too; re-derived from git log per the STOP condition | `grep -c Shipped` → 7 rows | | 3a3e138 |
| 006 | Step 3: README truth fixes | SHIPPED | diagram + bridge/android bullets pane-native (no `pi --mode rpc`); test-count line replaced with suite coverage; duplicate `## 7.` renumbered to unique 1–10; claude known-limits rewritten (transcripts exposed; real residual limits: no fork-at-path, no model catalog, control set abort/compact/close/set_model); dead RPC troubleshooting row dropped; 30s-poll row → lifecycle-scoped polling; config table extended with XDG_CONFIG_HOME, HERDR_SOCKET_PATH, COCKPIT_REPO_ROOTS, PI_CODING_AGENT_DIR, PI_CODING_AGENT_SESSION_DIR, CLAUDECONFIGDIR | | verification greps: one `## 7.`, no `mode rpc`, no `transcripts are not exposed`, no `36 tests`, COCKPIT_REPO_ROOTS documented | fresh-agent review: CLAUDECONFIGDIR missing from the env table (plan's item 9 list predates the claude adapter) → fixed in 6bd86dc | 3a3e138, 6bd86dc |
| 007 | Step 1: container access for components | SHIPPED | CockpitApp companion `container(context)` resolves the app container (safe on cold start: Application.onCreate runs before any component); services/receivers no longer build their own OkHttp stacks | CockpitApp.kt | compileDebugKotlin | fresh-agent review: none | 0a9ba9b |
| 007 | Step 2: monitor service uses the shared stack + live cursor | SHIPPED | CockpitMonitorService swaps its inline NtfyClient for an injectable ntfyClientFactory seam defaulting to the container; the poll cycle is extracted to suspend pollOnce() which reads store.ntfyCursor at the top of every cycle — fixes the live bug where lastId was read once before the 30s loop so every cycle re-fetched (and re-notified) since app start; CancellationException rethrown before the retry catch | CockpitMonitorService.kt | CockpitMonitorServiceTest (3: cursor read/advance, pane-less skip, empty cursor) | fresh-agent review: High cancellation-swallow fixed | 0a9ba9b |
| 007 | Step 3: reply receiver uses the shared stack, failures visible | SHIPPED | NotificationReplyReceiver swaps its container pull for an injectable bridgeProvider seam defaulting to the container; failures Log.w (was silent swallow); cancellation rethrown; test-only pendingResultSignal hook lets tests await finish() through the Robolectric shadow — no framework state retained in production | NotificationReplyReceiver.kt | NotificationReplyReceiverTest (2: steer WS frame carries pane id + trimmed text; 500 path awaits finish() + asserts the logcat warning) | fresh-agent review: two rounds, 9 findings total, all fixed (cancellation, finish() untested, seam restore, main-thread url(), prefs isolation, hook retention, unregistered receiver, teardown prefs, unbounded await) | 0a9ba9b |
| 007 | Step 4: NtfyClient dead cursor write | SHIPPED | The 'since = message.id' write is deleted — the caller owns the cursor — making `since` a val for the call's lifetime | NtfyClient.kt | NtfyClientTest (6: NDJSON order + keepalive/malformed skip, since in request path, non-2xx IOException, latestId, empty bodies) | fresh-agent review: clean | 0a9ba9b |
| 008 | Step 1: explicit live-suite opt-in | SHIPPED | herdr-client.test.ts liveSocketPath() uses HERDR_SOCKET_PATH ONLY (no silent default-path probe, matching server.integration.test.ts) and prints a skip notice when unset; server.integration.test.ts gate additionally requires the socket to exist and announces its own skip — a bogus HERDR_SOCKET_PATH now yields 279/279 green with both notices instead of a loud ENOENT failure | herdr-client.test.ts, server.integration.test.ts | npm test unset + bogus-path runs | **Scope deviation:** plan 008 lists server.integration.test.ts as out of scope, but its own test plan requires green with a skip notice on a nonexistent path — impossible without touching that file's gate; 3 gate lines changed, no test bodies; reviewer confirmed the change resolves the plan's internal contradiction | cd609bc |
| 008 | Step 2: pin the four unpinned routes | SHIPPED | 6 new offline cases through the real dispatcher: /api/models (200, ok:true, catalog present), /api/models?agent=nonsense (404, never 502), /api/commands?agent=nonsense (404 — commands.ts already maps it via plan 004), /api/agents/kinds (exact 5-field wire contract id/displayName/capabilities/hasModelCatalog/hasSlashCommands for both pi and claude — the NewSessionSheet.kt picker contract), /api/dirs (200 listing + 400 missing, fixture under homedir() with cleanup), /api/usage (200 shape); harness gains a minimal models-store.json fixture | server.test.ts | focused run 24/24 | fresh-agent review: Medium ok:true never asserted + Medium kinds field set not exact + Low home-dir litter — all fixed | cd609bc |
| 008 | Step 3: delete the double-only tests | SHIPPED | describe("fake herdr recording") removed — it asserted only on the fake herdr with no production code in the path; suite drops exactly 2 tests | ws-commands.test.ts | npm test count delta −2 | fresh-agent review: clean | cd609bc |
| 008 | Step 4: scripts/verify.sh | SHIPPED | One-command aggregate of the four AGENTS.md gates, fail-fast, --no-emulator skips the GMD suite; every gradle call bounded with shell `timeout` (gradle 8.13 has no --timeout flag — High review finding) | scripts/verify.sh (new, 755), AGENTS.md verification section | bash -n; scripts/verify.sh --no-emulator → all gates green | fresh-agent review: High unbounded gradle calls — fixed with shell timeouts | cd609bc |
| 009 | Fix A: review.ts diff truncation | SHIPPED | capUtf8 byte-slices the head (bytes.subarray(0, maxBytes)) dropping a straddling partial code point, mirroring live-output.ts's tail pattern; exported for the byte-precision test — a 64 KiB diff no longer halves down to ~2 KiB | review.ts, review.test.ts | review.test.ts 18/18; full suite 296 tests / 43 suites | fresh-agent review: no concrete findings on A–E, G, H | 251fdcc |
| 009 | Fix B: ntfy publish bound + throttle prune | SHIPPED | publish fetch gains AbortSignal.timeout(10s); NtfyPublisher.prune() mirrors StatusTracker.prune and is called from the server.ts pane-close branch with the live pane set — a closed pane's throttle is forgotten | notify.ts, server.ts, notify.test.ts | notify.test.ts 7/7 (throttled pane publishes again immediately after prune; alive panes keep throttle) | fresh-agent review: clean | 251fdcc |
| 009 | Fix C: attachment magic-byte sniffing | SHIPPED | storeAttachment sniffs the leading bytes (PNG/JPEG/GIF/WebP) and throws AttachmentError 400 on mismatch with the extension family — a client-declared image type cannot smuggle arbitrary bytes to the agent | attachments.ts, attachments.test.ts | attachments.test.ts 8/8 (fixtures upgraded to real magic bytes; mismatch + accept cases) | fresh-agent review: clean | 251fdcc |
| 009 | Fix D: attachment thumbnail stream | SHIPPED | AttachmentChip decodes bounds and pixels from two fresh content-resolver streams instead of reset() on a non-markable stream — the preview previously always fell into the catch and never rendered; manual/emulator evidence only (composable-internal decode, per plan) | ChatScreen.kt | compileDebugKotlin + assembleDebug | fresh-agent review: clean | 656bd7b |
| 009 | Fix E: dead exports | SHIPPED | rootWithTrailingSep + homeJoin (dirs.ts) and fileName (board-detail.ts) deleted after verifying zero references outside their definitions; unused basename import dropped | dirs.ts, board-detail.ts | typecheck exit 0 | fresh-agent review: clean | 251fdcc |
| 009 | Fix F: one relative-time formatter | SHIPPED | new ui/RelativeTime.kt relativeTime(epochMs, nowMs = now, dateAfterDays = null) as the superset ladder; BoardScreen.timeInState and HistoryScreen.relativeTime are thin wrappers (day-counting forever vs date after 7 days); wrappers take injectable nowMs so tests exercise the real call surface with a fixed instant; tests pin UTC (verified under TZ=Asia/Tokyo) | RelativeTime.kt (new), BoardScreen.kt, HistoryScreen.kt, BoardFormatTest.kt, HistoryFormatTest.kt | unit suite 167/167, assembleDebug | fresh-agent review: Medium timezone-dependent date assertion (failed under TZ=Asia/Tokyo) + Medium wrapper policy untested (tests bypassed the wrappers) — both fixed in 3780716 | 656bd7b, 3780716 |
| 009 | Fix G: pane-close prune symmetry | SHIPPED | No change needed — plan 001 already wired tracker.prune(livePanes) in the server.ts pane-close branch; fix verified as already landed | server.ts (unchanged) | grep tracker.prune in the pane-close branch | fresh-agent review: clean | (plan 001: a844b69) |
| 009 | Fix H: claude initial-prompt retry idempotent | SHIPPED | claudeDeliverInitialPrompt re-reads the pane (200-line window) BEFORE re-sending on every attempt after the first and never blind-resends when the read fails — a prompt that landed but whose echo scrolled away is no longer delivered up to 3 times; drop-recovery retry contract preserved | agents/claude/index.ts, agents-claude.test.ts | agents-claude.test.ts 25/25 (retry counts updated for check-before-resend; new cases: marker visible pre-send → no second prompt, unreadable pane → give up) | fresh-agent review: clean | 251fdcc |

## Verification gates

| Gate | Command | Last run | Result |
|------|---------|----------|--------|
| Bridge typecheck+tests | `cd bridge && npm run typecheck && npm test` | 2026-08-12 (plan 009) | 296 tests / 43 suites, 0 fail (279/279 with HERDR_SOCKET_PATH unset or bogus — both live suites announce their skip); typecheck exit 0 |
| Android unit tests | `cd android && ANDROID_HOME=$HOME/Android/sdk ./gradlew testDebugUnitTest --rerun-tasks` | 2026-08-12 (plan 009) | 167 tests, 0 fail (BUILD SUCCESSFUL) |
| Emulator instrumentation | `cd android && ANDROID_HOME=$HOME/Android/sdk ./gradlew pixel2api36DebugAndroidTest` | 2026-08-12 (plan 009) | 89/89, 0 fail |
| APK assemble | `cd android && ANDROID_HOME=$HOME/Android/sdk ./gradlew assembleDebug` | 2026-08-12 (plan 009) | BUILD SUCCESSFUL |

## Assumptions and blockers

- (none yet)

## Deferred notes

- `void line;` in `client.ts`'s subscribe data handler (line ends with `\r` branch)
  is a pre-existing dead statement; plan 009 candidate, deliberately not in 001
  scope.
